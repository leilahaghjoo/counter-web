version: "3.7"

services:
  my_nginx:
    image: nginx
    container_name: mynginx
     volumes:
       - app:/usr/share/nginx/html
    restart: always
    
  my_counter:
     build:
     container_name: mycounter
     volumes-from:
        - app
    
volumes:
   app: 
  
    
